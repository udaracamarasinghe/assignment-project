datasource:
  dbone:
    url: jdbc:mysql://localhost:3406/user_db?useSSL=false&allowPublicKeyRetrieval=true
    username: mysql
    password: password
    driverClassName: com.mysql.jdbc.Driver
    testWhileIdle: true
    validationQuery: SELECT 1 FROM DUAL
    maxActive: 5
    max-idle: 2
    min-idle: 1
    hibernate:
      dialect: org.hibernate.dialect.MySQL5InnoDBDialect
      show_sql: false
    schema: init-schema.sql
      
  dbtwo:
    url: jdbc:mysql://localhost:3406/user_db?useSSL=false&allowPublicKeyRetrieval=true
    username: mysql
    password: password
    driverClassName: com.mysql.jdbc.Driver
    testWhileIdle: true
    validationQuery: SELECT 1 FROM DUAL
    maxActive: 5
    max-idle: 2
    min-idle: 1
    hibernate:
      dialect: org.hibernate.dialect.MySQL5InnoDBDialect
      show_sql: false
  

jpa:
  hibernate:
    ddl-auto: validate
